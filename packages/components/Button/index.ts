import Button from './Button.vue';
import ButtonGroup from './ButtonGroup.vue'

import { withInstall } from '@yovy-ui/utils';

export const YoButton = withInstall(Button)
export const YoButtonGroup = withInstall(ButtonGroup)

export * from './types'